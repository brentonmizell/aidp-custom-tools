Created. One-page reference mirroring aidp_io/README.md style: public API table, endpoint contract for kb_search (POST URL + JSON body + response normalization shape), required conf keys table (region, data_lake_ocid, api_version=20260430, service_path=aiDataPlatforms, auth_mode, etc.), a 5-line worked example showing list_kbs + kb_search together, error envelope with try/except pattern (ValueError, ImportError, generic Exception), notable error shapes (404, HTTP, top_k bounds, missing aidp_io), and deployment note covering the SHARED_AIDP_KB_SRC / SHARED_AIDP_IO_SRC sync flow.